import { CustomerInfomation } from './customer-infomation';

describe('CustomerInfomation', () => {
  it('should create an instance', () => {
    expect(new CustomerInfomation()).toBeTruthy();
  });
});
